PGHOST=localhost
PGPORT=5432
PGUSER=demo
PGPASSWORD=demo
PGDATABASE=demo
PGSSLMODE=disable

# Enforce read-only at server level
READ_ONLY=true
QUERY_ROW_LIMIT=200

# No auth required; server is open within your network
